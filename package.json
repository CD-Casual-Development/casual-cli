{
  "name": "casual-cli",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=production bun run ./index.ts",
    "dev": "NODE_ENV=development bun run --watch ./index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
